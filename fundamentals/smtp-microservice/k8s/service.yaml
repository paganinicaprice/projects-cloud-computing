apiVersion: v1
kind: Service
metadata:
  name: smtp-service
spec:
  type: NodePort
  selector:
    app: smtp-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007  # Minikube używa NodePort
